# navTest 网格说明

`navTest.STL` 不在 git 中管理（大文件策略统一，见根目录 .gitignore `*.stl`）。

获取方式：
- 本地备份：`~/Documents/navTest.STL`
- 原始来源：SolidWorks 建模导出（狗洞测试地形，毫米单位 9000x1520x5500）

规格：450 面，单位毫米（model.sdf 中 `<scale>0.001` 转米），
模型坐标：x 0~9m 走廊长向，y 0~1.52m 宽向，z 0~5.5m，地面 z=0。
狗洞标称净空：0.25m / 0.26m。
